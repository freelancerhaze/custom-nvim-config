return {
  'nvim-treesitter/nvim-treesitter-textobjects',
  -- Its opts are typically configured within the main nvim-treesitter setup as shown above.
  -- You might only need this if your plugin manager requires separate plugin entries.
  -- If using a plugin manager like `lazy.nvim`, the dependency declaration inside
  -- the main `nvim-treesitter` table is often sufficient.
}
